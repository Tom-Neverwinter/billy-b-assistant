#########################################################################
# EXISTING CONFIGURATION (UNCHANGED) - Keep your current settings
#########################################################################

# OpenAI Configuration (existing - still works exactly as before)
OPENAI_API_KEY=sk-proj-...

# MQTT Configuration (existing)
MQTT_HOST=homeassistant.local
MQTT_PORT=1883
MQTT_USERNAME=billy
MQTT_PASSWORD=your_mqtt_password

# Home Assistant Configuration (existing)
HA_HOST=http://homeassistant.local:8123
HA_TOKEN=your_ha_token
HA_LANG=NL

# Voice Configuration (existing)
VOICE=ash

# Other existing settings (existing)
MIC_TIMEOUT_SECONDS=5
SILENCE_THRESHOLD=900
DEBUG_MODE=true
DEBUG_MODE_INCLUDE_DELTA=false
ALLOW_UPDATE_PERSONALITY_INI=true

#########################################################################
# NEW MULTI-PROVIDER CONFIGURATION (OPTIONAL)
# Only add these if you want to use different providers
#########################################################################

# LLM Provider Override (optional - defaults to openai if not set)
# LLM_PROVIDER=ollama                    # openai (default), ollama, kobold
# LLM_MODEL=llama3.2:latest             # Only needed for non-OpenAI
# LLM_API_URL=http://192.168.1.100:11434 # Only needed for non-OpenAI

# Voice Provider Override (optional - defaults to openai if not set)
# VOICE_PROVIDER=chatterai               # openai (default), chatterai, xtt
# VOICE_API_URL=http://192.168.1.101:8080 # Only needed for non-OpenAI

# Provider-specific settings (only add if using that provider)
# OLLAMA_HOST=192.168.1.100:11434
# KOBOLD_URL=http://192.168.1.102:5000
# CHATTERAI_API_KEY=your-chatterai-key
# XTT_API_URL=http://192.168.1.103:8080

# Advanced provider settings (optional)
# LLM_TEMPERATURE=0.7
# LLM_MAX_TOKENS=150
# VOICE_SPEED=1.0
